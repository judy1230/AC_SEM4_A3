<br />
<br />
<div class="row p-5">
		<div class="col-md-6">
			<img class="img-responsive center-block" src="{{user.image}}" style="width: 250px;margin-bottom: 25px;">
		</div>
		<div class="col-md-6">
			<h3>{{user.name}}</h3>
			<p>{{user.email}}</p>
			<a href="/users/{{user.id}}/edit"><button class="btn btn-outline-success my-2 my-sm-0">Edit</button></a>
		</div>
</div>

